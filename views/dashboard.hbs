<div style="margin-top: 5rem;">
    <div class="container-md mb-4">
        <div class="row">
            <div class="col-md-6"><h5 class="text-muted">Welcome: {{userName}}</h4></div>
            <div class="col-md-6 justisty-contentent-end">
                <a href="newBlog" class="float-end btn btn-sm btn-outline-dark">Create a new blog</a>
            </div>
        </div>
     <hr>
    </div>
   
    <div  class="container-md">
        {{!-- this to be added on a for loop --}}
        <div class="row g-3">
            {{#each blogs as |data|}}
            <a href="edit/{{data.dataValues.id}}" class="col-md-6 text-decoration-none text-dark">
                <div class="h-100 p-5 bg-light border rounded-3 shadow">
                    <h2>{{data.dataValues.title}}</h2>
                    <p>{{data.dataValues.content}}</p>
                </div>
            </a>
            {{/each}}
        </div>
    </div>
    <div class="d-flex justify-content-center mt-5 mb-5c">
        <a href="newBlog" class="btn btn-sm btn-outline-dark">Create a new blog</a>
    </div>
    </div>
</div>